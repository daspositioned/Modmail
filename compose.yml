services:
  bot:
    container_name: modmail
    image: seliba/modmail-bot:latest
    depends_on:
      - db
    restart: unless-stopped
    volumes:
      - ./.env:/app/.env
      - ./logs/:/app/logs/
    networks:
      - modmail
      
  db:
    container_name: modmail_db
    image: postgres:18.6
    restart: unless-stopped
    environment:
      - POSTGRES_USER=modmail
      - POSTGRES_PASSWORD=123456
      - POSTGRES_DB=modmail
    volumes:
      - ./data:/var/lib/postgresql/data
    networks:
      - modmail
    expose:
      - 5432
      
networks:
  modmail:
        external: false
        name: modmail_network
